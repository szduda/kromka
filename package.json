{
  "name": "kromka-zdrowia",
  "source": "site/index.html",
  "scripts": {
    "start": "parcel",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build --public-url .",
    "push-gh-pages": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose",
    "deploy": "yarn build && yarn push-gh-pages"
  },
  "devDependencies": {
    "parcel": "^2.9.3",
    "posthtml-modules": "^0.9.1",
    "shx": "^0.3.4"
  }
}
